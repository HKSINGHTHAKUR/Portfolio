/**
 * Root page — Server Component.
 *
 * The portfolio is no longer a stacked-section landing page. The universe
 * is persistent and spatial — destinations live as planets in 3-D space,
 * and the active destination's UI is rendered once, fixed-position, by
 * `PageContent` (mounted in the root layout).
 *
 * This page therefore renders nothing. Content lives on the planets.
 */
export default function HomePage() {
  return null
}
